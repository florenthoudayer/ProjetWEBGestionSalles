{% extends 'base.html.twig' %}
{% block title %}Formation{% endblock %}

{% block body %}
    <h1>Formation</h1>

    <table id="t2" class="table-bordered">
        <tbody>
            <tr>
                <th>Formation</th>
                <td>{{ formation.formation }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ formation.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a style="color: black;" href="{{ path('formation_index') }}">Retour à la liste</a>
        </li>
        <li>
            <a style="color: black;" href="{{ path('formation_edit', { 'id': formation.id }) }}">Modifier</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
