{% extends 'base.html.twig' %}

{% block body %}
    <h1>Titre</h1>

    <table>
        <tbody>
            <tr>
                <th>Titre</th>
                <td>{{ titre.titre }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ titre.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('titre_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('titre_edit', { 'id': titre.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
