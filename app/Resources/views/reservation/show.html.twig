{% extends 'base.html.twig' %}
{% block title %}Reservation{% endblock %}

{% block body %}
    <h1>Reservation</h1>

    <table id="t2" class="table-bordered">
        <tbody>
            <tr>
                <th>Datedebut</th>
                <td>{% if reservation.dateDebut %}{{ reservation.dateDebut|date('Y-m-d') }}{% endif %}</td>
            </tr>
            <tr>
                <th>Datefin</th>
                <td>{% if reservation.dateFin %}{{ reservation.dateFin|date('Y-m-d') }}{% endif %}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ reservation.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a style="color: black;" href="{{ path('reservation_index') }}">Retour à la liste</a>
        </li>
        <li>
            <a style="color: black;" href="{{ path('reservation_edit', { 'id': reservation.id }) }}">Modifier</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
