{% extends 'base.html.twig' %}
{% block title %}Matiere{% endblock %}

{% block body %}
    <h1>Matiere</h1>

    <table id="t2" class="table-bordered">
        <tbody>
            <tr>
                <th>Matiere</th>
                <td>{{ matiere.matiere }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ matiere.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a style="color: black;" href="{{ path('matiere_index') }}">Retour à la liste</a>
        </li>
        <li>
            <a style="color: black;" href="{{ path('matiere_edit', { 'id': matiere.id }) }}">Modifier</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
